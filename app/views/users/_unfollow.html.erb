<%= form_for current_user.relationships.find_by_followed_id(@user),
             :html => { :method => :delete },
             :remote => true do |ff| %>
  <div class="actions"><%= ff.submit "Unfollow" %></div>
<% end %>