<%= form_for current_user.relationships.build(:followed_id => @user.id),
             :remote => true do |ff| %>
  <div><%= ff.hidden_field :followed_id %></div>
  <div class="actions"><%= ff.submit "Follow" %></div>
<% end %>
